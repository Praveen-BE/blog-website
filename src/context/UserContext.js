// context/UserContext.js
import { createContext } from "react";

export const UserContext = createContext(null);


// If you have useAuth hook, keep it
export const useAuth = () => {
  // your hook code
}